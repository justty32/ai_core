## 0. 一頁摘要

- **初心**：未來聰明可靠的 LLM 會因 AI 泡沫化而變貴；假設手上只剩便宜的本地 <10B 小模型 → 必須做一套框架去**輔助小笨模型**，把它包裝成可靠的東西。CLI 天生適合 LLM，所以基本形態是「一堆小命令行函式 + 它們的組合」。
- **時間維度**：**趁現在聰明大模型還便宜，先用它生產出耐久的資產（程序 / snippet / few-shot / 骨架），讓未來的笨模型去消費。** 飛輪的第一圈要趁早轉。
- **終點形狀**：在成熟系統裡，**LLM 被擠到只剩兩個介面**——(1) 人類意圖 → 程式可理解意圖的**翻譯層**；(2) 尚未被固化的**模糊地帶的暫時處理者**。其餘全是確定性程式碼。
- **治理原則（拒絕為預設）**：LLM 預設什麼都不碰；只有**「千錘百鍊證明非它不可」＋「測得夠穩」**，才**核准**它進入某個環節，並發給一張**證書**（用哪個模型、測試組、穩定度 %）。舉證責任在「讓 LLM 進來」那邊，不在「把它拿掉」那邊。
- **由此得到的專案定義（升級版）**：
  > ai_core = **一個讓 LLM 退縮到「意圖翻譯」與「模糊前沿」兩介面的基底；它以「拒絕為預設、憑證准入」治理 LLM 的領地，並用飛輪持續把模糊地帶固化成確定性程式碼。**
- **第一個目標問題**：**程式碼輔助助手**——上層「讀框架原始碼 → 生資產」（＝結構化抽取），下層「在精準標記的位置填碼」（＝受約束生成）。
- **方法論剎車**：先選目標問題，再讓它告訴你哪些規範該先定。目標問題 = 停止鍵，是「東西愈做愈大」的解藥。

---

## 1. 初心與演化鏈

這個專案不是憑空長大的，它有一條有內在邏輯的演化鏈（這段以前只在腦子裡，今天落檔）：

1. **前提（約束）**：AI 泡沫化 → 聰明可靠的 LLM 太貴 → 假設只有便宜的本地 <10B 小笨模型可用。
2. **初心（最小形態）**：既然只能用小笨模型，就得做框架去**輔助**它；CLI 天生適合 LLM → 最初只是幾支小命令行程式，做 **prompt 片段管理 + 組合 + 輸出後處理**（＝最原始的 SFC）。
3. **第一次擴張**：推論要時間、一次只能回一串 → **singleton 資源** + 時間/記憶體管理（＝ LLM Entry Manager 的由來）。
4. **質變的領悟**：擴著擴著發現這形狀**像符號神經網路**——一張計算圖，多數節點是確定性的 shell/py 函式，少數節點是那個隨機的 LLM。
5. **野心的引爆點**：LLM 能自己生 prompt → **自我改進迴圈 → 數據飛輪**。為此東西從「幾支小程式」膨脹成「整套規範 + 框架 + 概念」。

> 設計哲學（KISS / Lightweight / No wheel-remake / Least dependency）不是美學偏好，是**前提的邏輯必然**：一個前提是「窮、只能用便宜算力」的專案，更付不起一座龐大基礎設施的維護成本。

---

## 2. 時間維度：資產工廠 vs 資產消費者

初心只講了「未來」的約束。今天補上的洞察是**現在這個窗口**的價值：

| | 聰明模型（現在便宜） | 笨模型（未來便宜） |
|---|---|---|
| 角色 | **資產工廠** | **資產消費者** |
| 跑法 | 貴、稀有、一次性 | 便宜、高頻 |
| 做什麼 | 讀框架原始碼、生程序/skill、生 snippet 庫、生 few-shot、搭骨架、**做固化（見 §3）** | 照著資產，在窄任務上填碼 / 套用 |

**策略**：把現在的「便宜智能窗口」當成一筆**一次性的資本投資**，產出折舊很慢的資產。聰明模型今天做好的東西，明天換成笨模型還能用。

此分工有兩個尺度：
- **per-phase（粗）**：工廠（一次性生資產）vs 消費者（高頻用資產）。
- **per-request（細）**：同一個請求內，**先笨模型填，沒過驗證再升級給聰明模型**——一條成本梯度（見 §3 的「洞」與升級）。
