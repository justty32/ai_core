先弄出一個基本的，好用的c abi，背後是C++實現。
然後弄出較高階的llm使用，主要是cli呼叫，同樣用C++。
然後是接入lua，還有fennel。
之後就要開始llm

一個會自我進化的agent循環
一個會固化llm呼叫的機制
一套好用的agent工具集

什麼是動態，其實動態本身也是檔案。只不過檔案描述的是待在那邊不動的，特定時間點下的某塊資料狀態。而動態，也就是可執行的東西，描述的是兩種資料狀態之間的變化關係，或者說函數。
unix哲學，linux檔案系統中，對檔案的描述已經很完整了，幾乎涵蓋了大部份靜態資料的描述。但對於動態這種函數資料的描述，卻仍然缺少很多。
對了，所謂的動態，他在執行檔狀態時，其所儲存的就是他的函數內容。但當我們執行了某個檔案，或者說cpu週期開始運轉，這些執行檔被載入到記憶體中，那就是開始運行其函數功能，也就是開始持續轉換當前檔案系統的狀態。
將其抽象化之後，暫時忽略一些太過細節太過底層的東西，比如記憶體和cpu週期，只看一個過程。我們可以發現，當我們啓動一個執行檔，等於我們親手推動了一次檔案狀態的變化。
而若是一個執行檔在變化檔案狀態時，我們加入其中進行參與...沒啥，反正就這樣。
然後套用到先前的"聯結即符號""符號即聯結"，假如一個符號就是一套檔案狀態，那麼兩套檔案狀態的聯結，就是一個函數，那既是檔案，也是聯結關係的顯化。

爲何我們要費這麼大力氣去做統一呢？因爲llm在使用時，上下文與環境變量愈單純，他就愈穩定

LLM本質上就是預測機器，以編碼器與解碼器的方式工作。他能做的只有幫忙編排與整理文字，還有快速將我們的想法轉化爲規整的文字，或是（經過特殊訓練後的LLM才有的功能）特定形式如程式碼。又或是將一大堆東西進行提取（注意力），能幫忙快速處理。
所以對於程式設計而言，本質上就是個超大型的自動補全機器，而且是有可能不可靠的那種。除此之外還可以用於程式碼庫快速查找與重點提取。還有程式碼結構檢查。
除此之外，由於用了大量語料，他還是個能快速查找訓練語料中資訊的百科全書。這也很好。
綜合來看，這是軟體工程的超級好工具。

