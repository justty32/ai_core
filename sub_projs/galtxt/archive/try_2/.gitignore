# build 產物全落 build/（vendor/lua/*.c/*.h 是 vendored 原始碼、host.cpp 是源碼，都進版控）
build/
*.exe
*.o

# compile_commands.json 是 build.sh 每次執行都重寫的 compilation database（給 clangd 用），
# 屬建置產物、內含機器上的絕對路徑，不進版控。
compile_commands.json

# 註：請求 body 暫存改寫到系統 TEMP，不在專案內；
#     test/fixtures/ 是離線測試資料（假回應），要進版控。
