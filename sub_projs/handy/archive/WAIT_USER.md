# WAIT_USER — 等待使用者的事

← [AGENTS.md](AGENTS.md)｜[INDEX](INDEX.md)

需要**使用者親自做 / 驗證**才能繼續的事——實機/實環境測試、外部服務登入、環境變數設定（如 `DEEPSEEK_API_KEY`）、權限操作、需帳號的下載、開/fork 另一個 agent 處理急件。Claude 能做結構性驗證＋打包到極限；跨不過去的那一關記這裡等使用者。

**只列還沒做的**——做完即移除（不留已完成清單，歷史看 git log）。

> **膨脹就拆**：待使用者項堆多了，就開 `wait_todo/` 按類別拆檔，本檔退回只留導航（照 [DEV-GUIDE](DEV-GUIDE.md)）。

## 待使用者項（open）

- （目前無）
