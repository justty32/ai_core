#!/usr/bin/env fennel
;; skills-json — 薄殼：載入庫、跑 (util.skills-json)（掃 skills/ → OpenAI tools JSON）。
(local fennel (require :fennel))
(local here (: (. arg 0) :match "^(.*)/[^/]*$"))
(set fennel.path (.. here "/src/?.fnl;" here "/../src/?.fnl;" fennel.path))
((. (require :util) :skills-json))
