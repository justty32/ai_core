#!/usr/bin/sbcl --script
;; skills-json — 薄殼：載入庫、跑 hermy:skills-json（掃 skills/ → tools JSON）。
(load (merge-pathnames "src/util.lisp"
        (make-pathname :directory (butlast (pathname-directory *load-truename*)))))
(hermy:skills-json)
