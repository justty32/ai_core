#!/usr/bin/sbcl --script
;; hermy — 薄殼：載入庫 src/util.lisp（package hermy）、跑編排器。邏輯全在庫裡。
(load (merge-pathnames "src/util.lisp"
        (make-pathname :directory (pathname-directory *load-truename*))))
(hermy:orchestrate)
